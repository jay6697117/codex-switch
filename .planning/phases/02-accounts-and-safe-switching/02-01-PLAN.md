---
phase: 02-accounts-and-safe-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.go
  - internal/contracts/
  - internal/accounts/
  - internal/settings/
  - frontend/src/lib/contracts.ts
  - frontend/src/lib/wails/services.ts
  - frontend/src/features/accounts/
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - ACCT-04
user_setup: []
must_haves:
  truths:
    - The app can load active and inactive accounts from a local store.
    - Renaming an account preserves its stable internal ID.
    - Deleting the active account automatically promotes a valid fallback when one exists.
    - Masking controls can be applied per account and globally in frontend state.
  artifacts:
    - Go account repository and CRUD use case implementation.
    - Typed account DTOs and Wails app-service facade methods.
    - Frontend account state module with active/other selectors and masking primitives.
  key_links:
    - Go repository results flow through Wails bindings into typed frontend services.
    - Active-account fallback is enforced in the Go service layer, not only in UI state.
---

<objective>
Build the account repository and CRUD foundation for Phase 2.

Purpose: establish stable account identity, active-account fallback rules, and the first real domain feature in the new Wails shell.
Output: Go account store/services, typed account contracts/facades, and frontend state hooks/selectors ready for UI integration.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accounts-and-safe-switching/02-CONTEXT.md
@app.go
@internal/bootstrap/service.go
@internal/contracts/bootstrap.go
@frontend/src/lib/contracts.ts
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src/components/AccountCard.tsx
@codex-switcher-origin/src/hooks/useAccounts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a local account repository with stable identity and fallback rules</name>
  <files>internal/accounts/, internal/contracts/, internal/settings/</files>
  <action>Create account domain models and repository persistence under Go, keeping a stable internal account ID separate from display name. Add active-account selection rules so deleting the active account auto-promotes the next available account and deleting the last account cleanly yields an empty state. Do not leak origin's Tauri command shape into the new domain model.</action>
  <verify>go test ./...</verify>
  <done>Repository tests prove add/list/rename/delete behavior, active-account fallback, and identity stability.</done>
</task>

<task type="auto">
  <name>Task 2: Expose account CRUD through typed Wails contracts and app services</name>
  <files>app.go, internal/contracts/, frontend/src/lib/contracts.ts, frontend/src/lib/wails/services.ts</files>
  <action>Add account DTOs, message/error contracts, and Wails binding methods for list/get active/rename/delete/mask-related state reads. Extend the existing typed facade rather than letting React features call raw bindings. Keep API names aligned with domain intent instead of copying Tauri invoke names blindly.</action>
  <verify>go test ./... && (cd frontend && npm run typecheck)</verify>
  <done>Frontend can call typed account facade methods without importing raw Wails bindings, and contract mismatches fail typecheck/tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend account state primitives for active/other grouping and masking</name>
  <files>frontend/src/features/accounts/, frontend/src/app/AppShell.tsx, frontend/src/components/</files>
  <action>Create an accounts feature module that normalizes active and inactive accounts, tracks global/per-account masking state, and prepares selectors/hooks for card rendering. Keep usage refresh, warm-up, and backup actions out of this slice so Phase 2 stays scoped to account management only.</action>
  <verify>cd frontend && npm test</verify>
  <done>Frontend tests prove active/other grouping, mask state behavior, and empty-state handling without reaching into raw runtime globals.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...`
- [ ] `cd frontend && npm run typecheck`
- [ ] `cd frontend && npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Account CRUD foundation exists and is ready for switch/process integration

</success_criteria>

<output>
After completion, create `.planning/phases/02-accounts-and-safe-switching/02-01-SUMMARY.md`
</output>
