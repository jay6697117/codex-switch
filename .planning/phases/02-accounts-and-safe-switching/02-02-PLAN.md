---
phase: 02-accounts-and-safe-switching
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app.go
  - internal/contracts/
  - internal/platform/
  - internal/switching/
  - frontend/src/lib/contracts.ts
  - frontend/src/lib/wails/services.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - USAGE-05
user_setup: []
must_haves:
  truths:
    - The app can report foreground and background Codex process state.
    - Switching accounts is blocked until the user explicitly confirms restart behavior when Codex is running.
    - The switch transaction rewrites auth context safely and restores prior running processes when requested.
  artifacts:
    - Go process detector and graceful stop/restart adapter.
    - Go switch transaction service with auth.json writer and structured errors.
    - Typed frontend facade for process status and switch requests.
  key_links:
    - Switch service uses the repository/account model created in 02-01.
    - Process state returned by Go is rendered through the same typed Wails facade boundary as other features.
---

<objective>
Build process detection and the safe switch transaction for Phase 2.

Purpose: make account switching trustworthy by treating running Codex processes and auth.json writes as a coordinated domain transaction instead of a plain file overwrite.
Output: Go process and switch services, structured contracts for process status/switch errors, and typed facades ready for UI flows.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accounts-and-safe-switching/02-CONTEXT.md
@.planning/phases/02-accounts-and-safe-switching/02-01-PLAN.md
@internal/bootstrap/service.go
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src-tauri/src/commands/process.rs
@codex-switcher-origin/src-tauri/src/auth/switcher.rs
@codex-switcher-origin/src-tauri/src/commands/account.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Codex process detection into Go platform adapters</name>
  <files>internal/platform/, internal/contracts/</files>
  <action>Implement process discovery and classification for foreground/background Codex processes, excluding the switcher itself. Preserve the origin distinction between visible Codex sessions and background IDE/extension helpers. Keep the adapter OS-aware and isolate shell command details behind a platform boundary.</action>
  <verify>go test ./...</verify>
  <done>Process detector tests cover no-process, foreground-only, and background-helper scenarios, and the contract clearly exposes counts and switch safety status.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the switch transaction and auth.json writer adapters</name>
  <files>internal/switching/, internal/platform/, internal/contracts/, app.go</files>
  <action>Build a switch use case that loads the selected account from the repository, requires explicit restart confirmation when processes are active, stops processes gracefully when approved, rewrites `auth.json` atomically under the correct `CODEX_HOME`, updates the active account, and restarts prior processes. Return structured errors/message codes instead of raw strings.</action>
  <verify>go test ./...</verify>
  <done>Switch tests cover running-process rejection, confirmed graceful restart, auth.json write success, and active-account update ordering.</done>
</task>

<task type="auto">
  <name>Task 3: Expose process status and switch APIs through typed frontend facades</name>
  <files>frontend/src/lib/contracts.ts, frontend/src/lib/wails/services.ts, app.go</files>
  <action>Add typed process-status and switch-request contracts to the existing frontend facade layer. Keep confirmation decisions in the UI, but keep safety enforcement in Go so direct binding misuse cannot bypass the rule.</action>
  <verify>go test ./... && (cd frontend && npm run typecheck)</verify>
  <done>Frontend receives typed process info and switch responses without importing runtime globals directly, and a missing confirmation path is rejected by Go.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...`
- [ ] `cd frontend && npm run typecheck`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Safe switch semantics are enforced in the domain layer and observable in contracts

</success_criteria>

<output>
After completion, create `.planning/phases/02-accounts-and-safe-switching/02-02-SUMMARY.md`
</output>
