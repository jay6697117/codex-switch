---
phase: 02-accounts-and-safe-switching
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - frontend/src/app/AppShell.tsx
  - frontend/src/components/
  - frontend/src/features/accounts/
  - frontend/src/i18n/
  - frontend/tests/e2e/
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - ACCT-04
  - AUTH-03
  - AUTH-04
  - USAGE-05
user_setup: []
must_haves:
  truths:
    - The shell shows the active account separately from other accounts.
    - Rename, delete, switch, and masking flows are usable from account cards.
    - Running Codex processes trigger an explicit switch-confirmation flow instead of a silent switch.
  artifacts:
    - Frontend account card/list feature integrated into the Phase 1 shell.
    - UI confirmation flow for switch safety and delete confirmation.
    - Frontend tests and smoke coverage for account rendering and switch confirmation.
  key_links:
    - UI actions are powered by typed account/process/switch facades from 02-01 and 02-02.
    - Process status is visible in the shell before a switch action is taken.
---

<objective>
Integrate account management and safe switching into the Wails shell UI.

Purpose: turn the repository and switch services from Plans 02-01 and 02-02 into a usable account-management surface that preserves origin parity without reintroducing a giant monolithic page.
Output: account feature UI, confirmation flows, shell integration, and targeted frontend/e2e regression coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accounts-and-safe-switching/02-CONTEXT.md
@.planning/phases/02-accounts-and-safe-switching/02-01-PLAN.md
@.planning/phases/02-accounts-and-safe-switching/02-02-PLAN.md
@frontend/src/app/AppShell.tsx
@frontend/src/lib/wails/services.ts
@frontend/src/i18n/createAppI18n.ts
@codex-switcher-origin/src/components/AccountCard.tsx
@codex-switcher-origin/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Phase 1 placeholders with account feature surfaces</name>
  <files>frontend/src/app/AppShell.tsx, frontend/src/features/accounts/, frontend/src/components/</files>
  <action>Integrate an accounts feature into the existing single-window shell, with a dedicated active-account section, an inactive-accounts section, and an empty-state view. Preserve high-density account card interactions from origin, but do not pull usage bars, warm-up buttons, or backup actions into this phase.</action>
  <verify>cd frontend && npm test</verify>
  <done>The shell renders active/inactive account sections, empty state, and process-state visibility without reverting to a monolithic page component.</done>
</task>

<task type="auto">
  <name>Task 2: Implement rename, delete, mask, and switch confirmation flows</name>
  <files>frontend/src/features/accounts/, frontend/src/i18n/, frontend/src/components/</files>
  <action>Build inline rename, delete confirmation, single/global masking controls, and a blocking confirmation flow for switching while Codex is running. Keep masking visible-by-default, and use structured error/message codes coming from the facade instead of raw English backend strings.</action>
  <verify>cd frontend && npm test</verify>
  <done>Frontend tests prove rename/delete/mask/switch-confirm flows, and UI does not allow a silent switch while running-process status is active.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused smoke coverage for the account management shell</name>
  <files>frontend/tests/e2e/, frontend/playwright.config.ts</files>
  <action>Add targeted browser-level smoke coverage that stubs Wails bindings to verify account list rendering, active/inactive grouping, and switch confirmation behavior. Keep the suite small and Phase-2-specific; do not expand into OAuth or usage flows yet.</action>
  <verify>cd frontend && npm run e2e</verify>
  <done>Playwright smoke proves the shell renders account data from the facade and blocks switch actions behind confirmation when process status indicates Codex is running.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run typecheck`
- [ ] `cd frontend && npm test`
- [ ] `cd frontend && npm run e2e`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Wails shell now supports account management and safe switching parity for Phase 2 scope

</success_criteria>

<output>
After completion, create `.planning/phases/02-accounts-and-safe-switching/02-03-SUMMARY.md`
</output>
