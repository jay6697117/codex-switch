# 05-03 Plan: Settings Section and Shell Synchronization

## Goal

新增独立 `SettingsSection`，完成 locale override、backup actions 与 shell revision 的前端同步闭环。

## Deliverables

- `SettingsSection` 仅包含 `Language` 与 `Backup & Security` 两块。
- locale 保存后立即切换前端 i18n，并更新 shell bootstrap 展示状态。
- slim/full import 成功后递增 shell revision，触发 `AccountSection` 重新 hydrate。
- `WarmupSection` 不新增第二个账号加载来源，继续只依赖 shell 传下来的账号快照。

## Verification

- `frontend/src/features/settings/SettingsSection.test.tsx`
- `frontend/src/app/AppShell.test.tsx`
- `frontend/tests/e2e/shell.smoke.spec.ts`
