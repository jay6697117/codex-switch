---
phase: 04-warmup-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.go
  - internal/accounts/
  - internal/auth/
  - internal/contracts/
  - internal/warmup/
  - frontend/src/features/accounts/
  - frontend/src/i18n/
  - frontend/src/styles/
  - frontend/src/lib/contracts.ts
  - frontend/src/lib/wails/bridge.ts
  - frontend/src/lib/wails/services.ts
autonomous: true
requirements:
  - WARM-01
  - WARM-02
user_setup: []
must_haves:
  truths:
    - Users can trigger a warm-up for one eligible account or all eligible accounts from the shell.
    - Manual warm-up execution is normalized into structured success/failure payloads instead of raw provider errors.
    - The UI can know when a warm-up action is unavailable and why, without guessing provider rules client-side.
  artifacts:
    - Go warm-up service and provider adapter.
    - Warm-up availability and result contracts on both Go and TypeScript sides.
    - Typed Wails facade plus account-shell wiring for single-account and all-account warm-up flows.
  key_links:
    - Warm-up execution reuses the account/auth materials built in Phases 2 and 3.
    - Manual warm-up contracts become the base dependency for scheduler-driven runs in 04-02.
    - Localized cross-source result feedback remains unified in 04-03.
---

<objective>
建立 Phase 4 的手动 warm-up 端到端能力。

目的：把“单账号 warm-up / Warm-up All / eligibility reason / normalized result”直接打通到现有账号区，尽早暴露 manual flow 的交互问题，而不是把 UI 集成拖到 scheduler 完成之后。
输出：Go warm-up service、manual warm-up Wails bindings、前端 typed contracts/facade、账号区动作接线，以及覆盖 eligibility / result / shell interaction 的测试。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-warmup-automation/04-CONTEXT.md
@.planning/phases/04-warmup-automation/04-RESEARCH.md
@app.go
@internal/usage/service.go
@internal/auth/token_service.go
@frontend/src/features/accounts/AccountSection.tsx
@frontend/src/lib/contracts.ts
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src-tauri/src/commands/usage.rs
@codex-switcher-origin/src-tauri/src/api/usage.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Go warm-up domain service and provider adapter</name>
  <files>internal/warmup/, internal/auth/, internal/accounts/</files>
  <action>新增 `internal/warmup`，实现单账号与全量 warm-up 执行器。它需要根据账号认证方式调用合适的 provider path，统一返回 per-account eligibility、执行结果和失败 reason code，并保持“手动 warm-up 不影响 scheduled completion state”的边界。先复用现有 auth/token/account 能力，不把 provider 细节泄露到 UI。</action>
  <verify>go test ./...</verify>
  <done>Go tests 覆盖 chatgpt/api key 两条 warm-up path、无凭据/不支持/账号不存在等 eligibility 结果，以及全量 warm-up summary 聚合。</done>
</task>

<task type="auto">
  <name>Task 2: Define manual warm-up contracts and expose Wails bindings</name>
  <files>internal/contracts/, app.go, frontend/src/lib/contracts.ts, frontend/src/lib/wails/bridge.ts, frontend/src/lib/wails/services.ts</files>
  <action>补充 manual warm-up 所需的 DTO：availability、single-result、all-result、per-account failure reason，并在 `app.go` 暴露 `WarmupAccount`、`WarmupAllAccounts` 和必要的 availability/status surface。前端只通过 typed facade 消费这些接口，不能直接依赖 runtime globals。</action>
  <verify>go test ./... && (cd frontend && npm run typecheck)</verify>
  <done>前后端 contracts 对齐，Wails facade 能稳定解包 warm-up 结果，且类型系统能表达 disabled reason / summary result。</done>
</task>

<task type="auto">
  <name>Task 3: Integrate manual warm-up actions into the existing account shell</name>
  <files>frontend/src/features/accounts/, frontend/src/i18n/, frontend/src/styles/</files>
  <action>在账号卡片里加入单账号 warm-up 动作，在顶部工具区加入 `Warm-up All`。按已锁定决策处理局部 loading 和 disabled reason，并把手动 warm-up 的结果状态接进账号区的前端状态模型；不要把 schedule 或 missed-run 逻辑提前混进来。</action>
  <verify>cd frontend && npm test</verify>
  <done>前端测试覆盖 single/all warm-up 触发、局部 loading、disabled reason、手动结果状态写回和 account shell 的最小交互闭环。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...`
- [ ] `cd frontend && npm run typecheck`
- [ ] `cd frontend && npm test`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- 所有任务完成
- 用户已经可以从 shell 触发单账号和全量 warm-up
- Go/Wails/TS contracts 和账号区动作对 disabled reason / normalized result 的边界已经稳定
- 没有把 schedule/missed-run 逻辑提前掺进 manual warm-up 基础能力

</success_criteria>

<output>
After completion, create `.planning/phases/04-warmup-automation/04-01-SUMMARY.md`
</output>
