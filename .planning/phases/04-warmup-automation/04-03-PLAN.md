---
phase: 04-warmup-automation
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - app.go
  - internal/contracts/
  - frontend/src/features/accounts/
  - frontend/src/features/warmup/
  - frontend/src/lib/wails/
  - frontend/src/i18n/
  - frontend/src/styles/
  - frontend/tests/e2e/
autonomous: true
requirements:
  - WARM-04
  - WARM-05
user_setup: []
must_haves:
  truths:
    - Users are interrupted by a missed-run prompt with `Run Now` / `Skip Today`.
    - Manual, scheduled, and missed-run warm-up outcomes are surfaced through localized shell feedback.
    - Runtime events are the single source of truth for background warm-up result delivery.
  artifacts:
    - Missed-run modal and recovery actions in the Warmup feature.
    - Localized feedback wiring for manual and scheduler-driven warm-up results.
    - Focused frontend/e2e coverage for runtime events, missed-run flows, and warm-up feedback.
  key_links:
    - UI behavior is powered exclusively by the typed warmup/scheduler facades from 04-01 and 04-02.
    - 04-03 must not re-open schedule editing or manual-action backend design work already settled earlier.
---

<objective>
把 missed-run 补跑与统一的本地化 warm-up 反馈接入当前 Wails shell。

目的：在 manual actions 和 schedule config 已经存在的前提下，完成 missed-run prompt、runtime event 消费和统一反馈层，让 Phase 4 的剩余用户可见能力闭环。
输出：missed-run prompt、localized feedback wiring、runtime event subscription、补跑动作，以及 focused frontend/e2e 回归。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-warmup-automation/04-CONTEXT.md
@.planning/phases/04-warmup-automation/04-RESEARCH.md
@.planning/phases/04-warmup-automation/04-01-SUMMARY.md
@.planning/phases/04-warmup-automation/04-02-SUMMARY.md
@app.go
@frontend/src/app/AppShell.tsx
@frontend/src/features/accounts/AccountSection.tsx
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src/components/ScheduledWarmupsModal.tsx
@codex-switcher-origin/src/components/MissedScheduledWarmupModal.tsx
@codex-switcher-origin/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire localized warm-up feedback into the shell</name>
  <files>internal/contracts/, frontend/src/features/accounts/, frontend/src/features/warmup/, frontend/src/lib/wails/, frontend/src/i18n/, frontend/src/styles/</files>
  <action>把 04-01 的手动 warm-up 结果与 04-02 的 scheduler runtime output 接到统一的 feedback 层，完成前后端都可对齐的 runtime event payload contract，并在 shell 中产出本地化的 toast 和轻量最近结果状态。UI 不能自行推导后台结果，必须消费 typed result payload 和 runtime event。</action>
  <verify>cd frontend && npm test</verify>
  <done>前端测试覆盖 manual/scheduled result 到 localized toast 与 recent-status 的映射，且结果来源统一走 typed payload / runtime event。</done>
</task>

<task type="auto">
  <name>Task 2: Build the missed-run prompt and recovery actions</name>
  <files>app.go, internal/contracts/, frontend/src/features/warmup/, frontend/src/lib/wails/, frontend/src/i18n/, frontend/src/styles/</files>
  <action>把 missed-run recovery actions 正式暴露给 shell：当天 missed-run 时显示阻断式 prompt，主动作是 `Run Now`，次动作 `Skip Today`。它必须通过 typed facade 调用本阶段新增的 recovery bindings，并严格保持“普通手动 warm-up 不自动抵扣 missed-run”的产品语义。</action>
  <verify>cd frontend && npm test</verify>
  <done>前端测试覆盖 missed-run prompt 的显隐、`Run Now` / `Skip Today` 动作、当天压提示与跨天重新计算的状态语义。</done>
</task>

<task type="auto">
  <name>Task 3: Add focused e2e coverage for warm-up automation feedback flows</name>
  <files>frontend/src/features/warmup/, frontend/src/lib/wails/, frontend/tests/e2e/, frontend/src/i18n/</files>
  <action>补 focused e2e 覆盖 runtime event feedback 和 missed-run recovery：验证 shell 在 scheduled result、missed-run prompt、`Run Now / Skip Today` 后保持一致状态，并确认已存在的 manual warm-up 和 schedule UI 不被回归破坏。</action>
  <verify>cd frontend && npm run e2e</verify>
  <done>Playwright smoke 证明 shell 可以消费 warm-up runtime events、显示 missed-run prompt，并在 `Run Now / Skip Today` 后保持一致的本地化反馈与摘要状态。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...`
- [ ] `cd frontend && npm run typecheck`
- [ ] `cd frontend && npm test`
- [ ] `cd frontend && npm run e2e`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- 所有任务完成
- missed-run prompt 和补跑动作已经接入 shell
- manual/scheduled/missed-run 的 warm-up 结果都通过统一的本地化反馈层呈现
- 运行时事件成为后台结果反馈的唯一来源，前端不自行推导 scheduler 语义

</success_criteria>

<output>
After completion, create `.planning/phases/04-warmup-automation/04-03-SUMMARY.md`
</output>
