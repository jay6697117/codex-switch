---
phase: 04-warmup-automation
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app.go
  - internal/contracts/
  - internal/settings/
  - internal/scheduler/
  - internal/warmup/
  - frontend/src/app/
  - frontend/src/features/warmup/
  - frontend/src/i18n/
  - frontend/src/styles/
  - frontend/src/lib/contracts.ts
  - frontend/src/lib/wails/bridge.ts
  - frontend/src/lib/wails/services.ts
autonomous: true
requirements:
  - WARM-03
user_setup: []
must_haves:
  truths:
    - The app can persist exactly one daily local-time warm-up schedule with selected account IDs.
    - Users can configure that schedule from a dedicated Warmup area with inline validation and account multi-select.
    - The backend can detect “missed today” and maintain the completion/suppression state that later prompt actions consume.
  artifacts:
    - Schedule settings store and status snapshot derivation.
    - Go scheduler runtime with ticker, session anchor, and missed-run semantics.
    - Typed Wails bindings/facades for schedule status and save operations, plus a dedicated Warmup UI for schedule read/write.
  key_links:
    - Scheduler execution must reuse the manual warm-up service from 04-01.
    - Missed-run prompt actions and localized runtime feedback remain reserved for 04-03.
---

<objective>
建立 Phase 4 的 schedule 配置能力，以及它所依赖的 scheduler 领域语义。

目的：把单条 daily schedule、本地时间持久化、摘要展示和配置交互尽早落成，同时把 missed-run 判定与 runtime event 语义稳定在 Go 侧，为 04-03 的 prompt/feedback 做准备。
输出：扩展后的 settings store、scheduler runtime、schedule status snapshot、schedule 配置 facade、独立 Warmup 区域与配置 modal，以及对应 tests。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-warmup-automation/04-CONTEXT.md
@.planning/phases/04-warmup-automation/04-RESEARCH.md
@.planning/phases/04-warmup-automation/04-01-SUMMARY.md
@internal/settings/store.go
@frontend/src/app/AppShell.tsx
@frontend/src/features/accounts/AccountSection.tsx
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src-tauri/src/scheduler.rs
@codex-switcher-origin/src-tauri/src/auth/settings.rs
@codex-switcher-origin/src-tauri/src/types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend settings persistence for daily warm-up schedule and derived status</name>
  <files>internal/settings/, internal/contracts/</files>
  <action>在现有 settings store 上加入 warm-up schedule schema，并实现读/写/更新/清洗逻辑。只持久化最小字段：enabled、local time、account ids、last run date、last missed-prompt date；`validAccountIds`、`missedRunToday`、`nextRunLocalIso` 在读取时动态计算。</action>
  <verify>go test ./...</verify>
  <done>Go tests 覆盖 schedule 保存校验、账号 ID 清洗、next-run 计算和 status snapshot 推导。</done>
</task>

<task type="auto">
  <name>Task 2: Implement scheduler runtime and missed-run semantics in Go</name>
  <files>internal/scheduler/, internal/warmup/, internal/contracts/, app.go</files>
  <action>新增 scheduler runtime：在 app startup 后启动 ticker，基于本地时间和 session start 判定 scheduled run 与 missed-run。它必须严格保持“手动 warm-up 不抵 scheduled completion”的边界，并维护 same-day suppression / completion markers 供 04-03 的 prompt action 消费；用户可见的 missed-run prompt / localized feedback 仍在 04-03 落地。</action>
  <verify>go test ./...</verify>
  <done>Scheduler tests 覆盖 app-open-only 触发、missed-run 判定、same-day suppression / completion markers，以及 scheduled execution 的前置语义。</done>
</task>

<task type="auto">
  <name>Task 3: Build the dedicated Warmup feature for schedule configuration</name>
  <files>app.go, frontend/src/app/, frontend/src/features/warmup/, frontend/src/lib/contracts.ts, frontend/src/lib/wails/bridge.ts, frontend/src/lib/wails/services.ts, frontend/src/i18n/, frontend/src/styles/</files>
  <action>把 schedule 相关能力暴露给前端：读取当前 schedule status、保存 schedule。shell 中需要加入独立 Warmup 区域，展示每日时间、参与账号数和 `Next run`，并提供 enable toggle、本地时间、多选清单、`Select All / Clear All` 与内联校验。missed-run prompt、recovery actions 和 runtime feedback 不在本任务落地。</action>
  <verify>cd frontend && npm test</verify>
  <done>前端测试覆盖 Warmup summary、schedule modal、内联校验、摘要更新与账号多选交互，且 UI 只消费 typed schedule contracts 与 facade。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...`
- [ ] `cd frontend && npm run typecheck`
- [ ] `cd frontend && npm test`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- 所有任务完成
- 用户已经可以配置并看到单条 global daily schedule 的摘要与设置界面
- schedule 持久化不保存派生状态，前端只消费 snapshot
- missed-run 和 runtime event 的后端语义已经就位，04-03 只需接 prompt 与 localized feedback

</success_criteria>

<output>
After completion, create `.planning/phases/04-warmup-automation/04-02-SUMMARY.md`
</output>
