---
phase: 03-oauth-and-usage-visibility
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - frontend/src/app/
  - frontend/src/components/
  - frontend/src/features/accounts/
  - frontend/src/features/auth/
  - frontend/src/features/usage/
  - frontend/src/i18n/
  - frontend/tests/e2e/
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - USAGE-01
  - USAGE-02
  - USAGE-03
  - USAGE-04
user_setup: []
must_haves:
  truths:
    - Users can add a new account through the Wails shell with an OAuth-only add-account flow.
    - Users can refresh usage for one account or all accounts and see normalized 5-hour/weekly data.
    - Usage unavailable/unsupported states are rendered through localized fallback copy.
  artifacts:
    - Frontend add-account OAuth flow integrated into the Phase 2 account shell.
    - Usage presentation components and refresh interactions.
    - Focused frontend/e2e coverage for OAuth and usage shell flows.
  key_links:
    - UI actions are powered exclusively by typed OAuth/usage facades from 03-01 and 03-02.
    - The Add Account surface remains OAuth-only and does not reintroduce Import File.
---

<objective>
把 OAuth 添加账号和 usage 展示整合进当前 Wails shell。

目的：在不破坏 Phase 2 单窗口架构的前提下，把“添加账号”和“查看/刷新 usage”变成真正可操作的用户流。
输出：OAuth-only add-account surface、usage cards/bars、refresh flows、本地化 fallback，以及针对壳层关键路径的测试。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-oauth-and-usage-visibility/03-CONTEXT.md
@.planning/phases/03-oauth-and-usage-visibility/03-01-PLAN.md
@.planning/phases/03-oauth-and-usage-visibility/03-02-PLAN.md
@frontend/src/app/AppShell.tsx
@frontend/src/features/accounts/AccountSection.tsx
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src/components/AddAccountModal.tsx
@codex-switcher-origin/src/components/UsageBar.tsx
@codex-switcher-origin/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate an OAuth-only add-account flow into the existing shell</name>
  <files>frontend/src/app/, frontend/src/features/auth/, frontend/src/features/accounts/, frontend/src/components/</files>
  <action>在现有 shell 中加入 Add Account 入口和 OAuth-only modal/flow，覆盖 `start -> browser handoff -> waiting -> complete/cancel/error`。明确不恢复 Import File tab。Pending/cancel 状态要能和当前 shell 共存，不能把页面变回大一统状态机。</action>
  <verify>cd frontend && npm test</verify>
  <done>Frontend tests 覆盖 Add Account 入口、pending OAuth、cancel、error 和成功后 snapshot 刷新。</done>
</task>

<task type="auto">
  <name>Task 2: Add usage presentation and refresh interactions</name>
  <files>frontend/src/features/accounts/, frontend/src/features/usage/, frontend/src/i18n/, frontend/src/components/</files>
  <action>给 account cards 接入 usage bar / fallback UI / refresh action，支持单账号刷新和全量刷新。UI 只消费 normalized usage snapshot，不在组件里解释 provider 差异。</action>
  <verify>cd frontend && npm test</verify>
  <done>Frontend tests 覆盖 single/all refresh、usage fallback、weekly/5h rendering、loading state 和错误映射。</done>
</task>

<task type="auto">
  <name>Task 3: Add targeted shell smoke coverage for OAuth and usage</name>
  <files>frontend/tests/e2e/, frontend/playwright.config.ts</files>
  <action>用 stubbed Wails bindings 为 shell 增加 OAuth 启动/取消和 usage refresh 的 focused smoke coverage。保持 suite 小而尖锐，不扩到 warm-up/backup。</action>
  <verify>cd frontend && npm run e2e</verify>
  <done>Playwright smoke 证明 shell 可以启动 OAuth add-account flow、渲染 usage、并在 refresh/cancel 场景下保持一致状态。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run typecheck`
- [ ] `cd frontend && npm test`
- [ ] `cd frontend && npm run e2e`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- 所有任务完成
- OAuth 添加账号闭环接入 shell，且没有 Import File 回归
- usage 展示与 refresh 已可操作且有本地化 fallback
- 前端仍然只通过 typed facade 消费 Phase 3 能力

</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-and-usage-visibility/03-03-SUMMARY.md`
</output>
