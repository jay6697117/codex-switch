---
phase: 03-oauth-and-usage-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app.go
  - internal/contracts/
  - internal/auth/
  - internal/usage/
  - frontend/src/lib/contracts.ts
  - frontend/src/lib/wails/services.ts
autonomous: true
requirements:
  - USAGE-01
  - USAGE-02
  - USAGE-03
  - USAGE-04
user_setup: []
must_haves:
  truths:
    - The app can fetch normalized 5-hour and weekly usage data for supported accounts.
    - ChatGPT usage requests retry once after a token refresh on unauthorized responses.
    - Unsupported or unavailable usage states are normalized into localized-friendly fallback payloads.
  artifacts:
    - Go usage adapter/service layer.
    - Token-freshness helpers reused by OAuth-created ChatGPT accounts.
    - Typed contracts/facades for single-account and bulk refresh.
  key_links:
    - Usage services reuse the account/auth model from 03-01 instead of inventing a second credential path.
    - Frontend receives normalized usage snapshots, not provider-specific JSON.
---

<objective>
实现 usage adapter、token freshness 和 refresh normalization。

目的：把 ChatGPT usage API 的不稳定边界、token refresh、unsupported fallback 收敛到 Go service 层，让前端始终消费统一 snapshot。
输出：usage service、token freshness helpers、single/all refresh contracts，以及结构化 fallback/result 语义。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-oauth-and-usage-visibility/03-CONTEXT.md
@.planning/phases/03-oauth-and-usage-visibility/03-01-PLAN.md
@internal/accounts/
@internal/auth/
@frontend/src/lib/contracts.ts
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src-tauri/src/api/usage.rs
@codex-switcher-origin/src-tauri/src/auth/token_refresh.rs
@codex-switcher-origin/src-tauri/src/commands/usage.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build usage normalization and fallback contracts</name>
  <files>internal/usage/, internal/contracts/</files>
  <action>定义新项目 usage snapshot/result 结构，统一承载 5-hour、weekly、reset time、plan type 和 unavailable/unsupported fallback。不要把 provider 原始字段直接暴露给前端。</action>
  <verify>go test ./...</verify>
  <done>Go tests 覆盖 supported payload、empty payload、unsupported account 和 unavailable fallback 的归一化结果。</done>
</task>

<task type="auto">
  <name>Task 2: Implement token freshness and refresh-aware usage service</name>
  <files>internal/auth/, internal/usage/</files>
  <action>实现 access token 过期判断、refresh token 刷新、active account auth.json 同步，以及 usage `401 -> refresh -> retry once`。对于 API key 账号，明确返回 unsupported，而不是让 UI 猜测。</action>
  <verify>go test ./...</verify>
  <done>Go tests 覆盖 near-expiry refresh、401 retry、active account sync、API key unsupported、network/provider failure fallback。</done>
</task>

<task type="auto">
  <name>Task 3: Expose single-account and bulk usage refresh through typed facades</name>
  <files>app.go, internal/contracts/, frontend/src/lib/contracts.ts, frontend/src/lib/wails/services.ts</files>
  <action>新增 `GetAccountUsage`、`RefreshAllUsage` 或等价 typed contract/facade。bulk refresh 只返回归一化 usage snapshot，不把并发或 retry 逻辑泄露给前端。</action>
  <verify>go test ./... && (cd frontend && npm run typecheck)</verify>
  <done>前端 typed facade 已能拿到单账号和全量 usage 结果；contract mismatch 会在 typecheck/tests 中失败。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...`
- [ ] `cd frontend && npm run typecheck`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- 所有任务完成
- usage 查询的 refresh / retry / unsupported 语义封装在 Go 层
- 前端拿到统一 snapshot，而不是 provider 原始响应
- usage fallback 天然可接入 i18n

</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-and-usage-visibility/03-02-SUMMARY.md`
</output>
