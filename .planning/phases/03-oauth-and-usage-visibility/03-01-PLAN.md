---
phase: 03-oauth-and-usage-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.go
  - internal/accounts/
  - internal/contracts/
  - internal/auth/
  - frontend/src/lib/contracts.ts
  - frontend/src/lib/wails/services.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - The app can start a ChatGPT OAuth flow and expose the generated browser URL through a typed contract.
    - Only one OAuth login flow can be pending at a time, and cancel/timeout leave no broken pending state behind.
    - Completing OAuth materializes a new account into the local store, makes it active, and syncs Codex auth context safely.
  artifacts:
    - Go OAuth callback server and pending-flow manager.
    - Go account-creation service that writes the new account into the Phase 2 repository.
    - Typed Wails facade methods for start, complete, and cancel OAuth login.
  key_links:
    - OAuth account creation reuses the stable account repository and switch transaction built in Phase 2.
    - Frontend continues to consume typed facades rather than raw runtime globals.
---

<objective>
实现 ChatGPT OAuth callback server、pending flow 管理和账号创建服务。

目的：把“浏览器登录 -> 本地回调 -> token exchange -> 新账号落库 -> 激活并同步 auth.json”这条高风险链路封装成稳定的 Go service，而不是把临时状态散落在前端。
输出：OAuth service、pending flow manager、typed contracts/facades，以及可验证的取消/超时恢复语义。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-oauth-and-usage-visibility/03-CONTEXT.md
@app.go
@internal/accounts/
@internal/switching/
@frontend/src/lib/contracts.ts
@frontend/src/lib/wails/services.ts
@codex-switcher-origin/src-tauri/src/commands/oauth.rs
@codex-switcher-origin/src-tauri/src/auth/oauth_server.rs
@codex-switcher-origin/src-tauri/src/auth/token_refresh.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build OAuth callback server and pending-flow manager</name>
  <files>internal/auth/, internal/contracts/</files>
  <action>实现 PKCE、state、默认端口 1455 + fallback、本地 callback server、timeout 和 cancel 语义。显式建模 pending OAuth flow，确保新 flow 启动时会清理旧 flow，不留下端口占用或悬挂 receiver。</action>
  <verify>go test ./...</verify>
  <done>Go tests 覆盖 pending flow 替换、cancel、timeout、state mismatch、callback success/error 分支。</done>
</task>

<task type="auto">
  <name>Task 2: Materialize OAuth result into a stored account and active auth context</name>
  <files>internal/auth/, internal/accounts/, internal/switching/, app.go</files>
  <action>把 token exchange 结果转换成新项目私有账号模型，落入本地仓库，设为 active account，并通过既有 switch boundary 同步 `auth.json`。禁止复制 origin 的 Tauri 命令式耦合；在 Go service 层返回结构化错误。</action>
  <verify>go test ./...</verify>
  <done>完成 OAuth 后能得到新账号 snapshot；取消/失败时不会生成半成品账号；active context 与本地 store 一致。</done>
</task>

<task type="auto">
  <name>Task 3: Expose OAuth flow through typed Wails facades</name>
  <files>app.go, internal/contracts/, frontend/src/lib/contracts.ts, frontend/src/lib/wails/services.ts</files>
  <action>新增 `start / complete / cancel` OAuth contract 和 typed frontend facade。前端只拿到浏览器 URL、pending 状态与结构化错误码，不接触 provider 细节或本地 server 生命周期。</action>
  <verify>go test ./... && (cd frontend && npm run typecheck)</verify>
  <done>前端可通过 typed facade 驱动 OAuth flow；缺失 binding 或 contract mismatch 会在 tests/typecheck 中失败。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...`
- [ ] `cd frontend && npm run typecheck`
- [ ] `wails build -clean`
</verification>

<success_criteria>

- 所有任务完成
- OAuth flow 的 cancel / timeout / success 语义稳定
- 新账号创建复用 Phase 2 的账号仓库与 auth context 更新边界
- 无原始英文 provider 错误直接暴露到前端

</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-and-usage-visibility/03-01-SUMMARY.md`
</output>
